#include <iostream>
#include<stack>
#include <vector>
using namespace std;
int main()
{
    int array[]={2,5,8,1,23,-2};
    int n = sizeof(array) / sizeof(array[0]);
    for(int i=0;i<n;i++)
    {
    }
    
}